export enum Roles {
    User,
    Supervisor
}

export enum EFormat {
    Conference, 
    Lecture,
    Workshop, 
    Fest
}

export enum ESorting {
    created_at = "date",
    name = "name",
    ticket_price = "ticket_price",
    ticket_count = "ticket_count"
}